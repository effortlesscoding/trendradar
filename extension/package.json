{
  "name": "trendradar-extension",
  "version": "1.0.0",
  "description": "Reddit research tool Chrome extension for YouTube content creators",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node --watch scripts/build.mjs",
    "preview": "vite build --mode development",
    "deploy": "node scripts/deploy.js",
    "build:deploy": "npm run build && npm run deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.300",
    "archiver": "^7.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.3.0"
  }
}
