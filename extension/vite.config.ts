// Build is handled by scripts/build.mjs (separate IIFE bundle per entry).
// This file is kept for editor tooling (type checking, etc.).
import { defineConfig } from 'vite'
export default defineConfig({})
